- add circuit extraction
- add "auto-simplifying" `add_wire` (will likely need to restrict to ZX only)
- work on Clifford+T rules
    - add a framework for taking superpositions of diagrams via
      "Speedy Contraction..."
