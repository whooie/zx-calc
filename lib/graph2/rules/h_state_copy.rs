use super::*;

/// A unary H-box of argument -1 or Z-spider of phase π connected to an H-box of
/// arbitrary arity and argument -1.
#[derive(Copy, Clone, Debug)]
pub struct HStateCopy(NodeId, NodeId);
//         HStateCopy(state,  hcopier)


