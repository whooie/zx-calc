//! Alternative interface to [`Diagram`][crate::ketbra2::Diagram] from a
//! circuit-based context.
//!
//! The default gate set is defined via `Gate`, but can be extended using the
//! `GateDiagram` trait. See `circuit_diagram!` for example usage and
//! abbreviated syntax.
